encoding: utf-8

# Site settings
window_title: 🐻
title: Hello there!
email: 
description: ""
baseurl: ""
url: ""
twitter_username: nealxyc
github_username:  nealxyc

# Build settings
markdown: kramdown
permalink: posts/:categories/:year/:month/:day/:title/

# kramdown config
kramdown:
  input: GFM
  coderay_line_numbers: nil

# Deployment temp files
exclude: [deploy.sh, deploy, credential, cred, bower.json, package.json, Gruntfile.js, node_modules, bower_components]

# post format
defaults:
  -
    scope:
      path: "_posts"
      type: "post"
    values:
      layout: "post"

collections:
  projects:
    output: true

